# example_card/

Reference lorebooks used as e2e/unit fixtures: the real-world Fuyuki lorebook (large, exotic vendor fields — the lossless round-trip stress test), the small `Example test lorebook.json`, and `linter-demo.lorebook.json` (seeded with lint findings).

**Hints**

- Changes here can pin test behavior — grep `e2e/` and `**/*.spec.ts` before editing.
