# sillytaver-world-info-doc/

Vendored SillyTavern World Info reference: `world-info.js` (the actual upstream implementation) and `worldinfo.md`. Read-only ground truth — `core/models/st-regex.ts` and `st-key-match.ts` are pure ports of it and cite it by line number.

**Hints**

- Treat as upstream snapshot: don't edit, and re-verify the line-number citations in core models if it is ever refreshed.
