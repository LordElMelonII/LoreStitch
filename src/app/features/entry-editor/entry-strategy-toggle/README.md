# entry-strategy-toggle/

The segmented toggle group for an entry's activation strategy (Normal 🟢, Constant 🔵, Vector 🔗).

Renders inline within the accordion trigger strip on desktop viewports and as a dedicated section in the expandable options panel on mobile viewports. Dispatches mutations through `EntryUpdatesService.setTriggerState`.
