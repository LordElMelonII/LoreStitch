# entry-editor/entry-placement/

Insertion position pane: every SillyTavern position (before/after char defs, before/after example messages, top/bottom of author's note, `@ Depth` with chat depth + role, manual Outlet entries).
