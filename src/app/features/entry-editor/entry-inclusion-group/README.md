# entry-editor/entry-inclusion-group/

Inclusion-group pane: comma-separated group labels + group weight, stored exactly as SillyTavern keeps them, controlling how entries of one group compete for insertion.
