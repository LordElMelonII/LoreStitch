# shell/topbar/

Top application bar: project badge + switcher, commit/dirty indicator, token meter (`token-meter.ts`) with its inspector dialog, health-check shield (badge = open lint issue count), focus-mode toggle, theme and action menus.
