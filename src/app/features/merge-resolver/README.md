# features/merge-resolver/

`merge-resolver-dialog.ts` — import another lorebook and resolve clashes entry-by-entry (import / overwrite / skip, with diff previews). `export-selected-dialog.ts` — pick a subset of entries to export. Both are dual-container panes; selection models in `*.model.ts`.
