# shared/components/diff-viewer/

Side-by-side diff renderer with previous/next change navigation; consumes precomputed diff hunks (`diff-viewer.model.ts`) — used by commit history and the merge/delimiter previews.
