/**
 * The compact-phone breakpoint. The shell (`App`), the options accordion and
 * the project actions picker all key responsive behavior off it, so the media
 * query lives in exactly one place.
 */
export const MOBILE_BREAKPOINT_QUERY = '(max-width: 767px)';
