# shared/constants/

App-wide constants: `breakpoints.ts` (handset/tablet breakpoints), `touch-targets.ts` (44px floor, 48px on phones), `github.ts` (repo links + icon), `search.ts` (200ms search debounce shared by the sidebar filter and the Search & Replace dialog).
